{
  "buildCommand": "pnpm --filter @airang/web build",
  "outputDirectory": ".next",
  "installCommand": "pnpm install --ignore-scripts",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/reminders",
      "schedule": "0 0 * * *"
    }
  ]
}
